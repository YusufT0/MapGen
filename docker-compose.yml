version: '3.8'

services:
  mapgen-backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mapgen_backend
    ports:
      - "8000:8000"
    volumes:
      - ./uploads:/app/uploads
      - ./maps:/app/maps
      - ./configs:/app/configs
    restart: unless-stopped
